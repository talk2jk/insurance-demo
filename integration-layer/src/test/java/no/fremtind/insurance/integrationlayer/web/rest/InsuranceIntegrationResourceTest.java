package no.fremtind.insurance.integrationlayer.web.rest;

import no.fremtind.insurance.integrationlayer.IntegrationLayerApplication;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

import static org.junit.Assert.*;

@RunWith(SpringRunner.class)
@SpringBootTest(classes = IntegrationLayerApplication.class)
public class InsuranceIntegrationResourceTest {

    @Before
    public void setUp() throws Exception {
    }

    @Test
    public void createInsurance() {
    }
}